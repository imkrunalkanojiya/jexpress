package com.jexpress;

/**
 * Enum representing HTTP methods supported by the REST API framework.
 */
public enum HttpMethod {
    GET,
    POST,
    PUT,
    DELETE,
    PATCH,
    HEAD,
    OPTIONS
}